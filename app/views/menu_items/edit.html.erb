<%="Edit Menu Item"%><br><br>

  <%= render partial: "layouts/errors", locals: {object: @menu_item}%>

  <%= form_with model: @menu_item, local: true do |f| %>

  <%= render partial: "item_fields", locals: {f: f}%>

  <%end%>

  <%=button_to "Delete", menu_item_path(@menu_item), method: :delete%>
